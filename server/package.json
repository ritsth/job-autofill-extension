{
  "name": "job-autofill-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloud Run proxy that relays generation requests to Vertex AI (Gemini). Keeps the GCP credential server-side; identifies each caller by their Google sign-in token and enforces a per-user daily quota in Firestore. A shared admin token bypasses sign-in + quota.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.7.1",
    "@google-cloud/vertexai": "^1.9.3"
  }
}
